# Generated Deployment Pipeline

# Deployment Job for tenant1
deploy-tenant1:
    stage: deploy
    script:
        - ./deploy
    variables:
        INPUT_MANIFEST: "manifests/default-manifest.yaml"
        TENANT_NAME: "tenant1"
        ENVIRONMENT: "staging"
        TENANT_ID: "tenant1"
        CUSTOM_SETTING: "enabled"

# Deployment Job for tenant2
deploy-tenant2:
    stage: deploy
    script:
        - ./deploy
    variables:
        INPUT_MANIFEST: "manifests/default-manifest.yaml"
        TENANT_NAME: "tenant2"
        ENVIRONMENT: "production"
        TENANT_ID: "tenant2"
        CUSTOM_SETTING: "disabled"
